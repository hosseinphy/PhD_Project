to generate new random dist. : set fract (\Phi_s) and N_s and system sizes 
to compile it : icc -openmp -O3 main.cpp -lRandom 
qsub script.sh 

You should install lRandom library on guillimin (will teach you later)
